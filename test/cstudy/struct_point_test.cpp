#include <stdio.h>
#include <string.h>


struct student
{
	int num;
	char name[20];
	char sex;
}*p2;


// 结构体指针  是指向结构体的首地址
/**
 * 结构体指针的访问变量方法
    1）p->结构体成员；
    2）(*p).结构体成员；
 */
int main(int argc, char const *argv[])
{
    
    
    return 0;
}
